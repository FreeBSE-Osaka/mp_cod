fine_tune_type: lora
optimizer: adamw
mask_prompt: true
num_layers: 8
batch_size: 1
iters: 96
val_batches: -1
learning_rate: 5.0e-5
steps_per_report: 12
steps_per_eval: 24
grad_accumulation_steps: 4
save_every: 96
max_seq_length: 1152
grad_checkpoint: false
clear_cache_threshold: 0
seed: 20260951
lora_parameters:
  rank: 8
  dropout: 0.0
  scale: 16.0
