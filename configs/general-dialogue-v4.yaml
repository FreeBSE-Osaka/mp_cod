fine_tune_type: lora
optimizer: adamw
mask_prompt: true
num_layers: 8
batch_size: 1
iters: 160
val_batches: -1
learning_rate: 2.0e-5
steps_per_report: 20
steps_per_eval: 40
grad_accumulation_steps: 4
save_every: 80
max_seq_length: 640
grad_checkpoint: false
clear_cache_threshold: 0
seed: 20260903
lora_parameters:
  rank: 8
  dropout: 0.0
  scale: 16.0
