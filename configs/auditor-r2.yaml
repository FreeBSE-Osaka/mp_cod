fine_tune_type: lora
optimizer: adamw
mask_prompt: true
num_layers: 4
batch_size: 1
iters: 12
val_batches: 2
learning_rate: 1.0e-5
steps_per_report: 1
steps_per_eval: 4
save_every: 4
max_seq_length: 576
grad_checkpoint: false
clear_cache_threshold: 0
seed: 20260826
lora_parameters:
  rank: 4
  dropout: 0.0
  scale: 8.0
